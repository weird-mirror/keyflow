import Foundation

// Central place for all external URLs. Replace the placeholders below once the
// Buy Me a Coffee page, GitHub repo and domain are live.
enum Links {
    static let donate = URL(string: "https://buymeacoffee.com/weird_mirror")!

    static let repo = URL(string: "https://github.com/weird-mirror/keyflow")!

    // TODO: replace with the real landing page domain once purchased.
    static let website = URL(string: "https://keyflow.app")!
}
